import { createSlice } from "@reduxjs/toolkit";

const CityList = createSlice({
    name:"CityList",
    initialState:{
        CList : []
    },
    reducers:{
        
    }
})

const {} = CityList.actions
const reducer = CityList.reducer

export {}
export default reducer